export const PoliciesContent = (item) => {
    return (<>
        <p>Your Leave Quota Reset Based On {item.resetBase}.</p>
        <p>You can Asign Leave Policies Here</p></>
    )
}

export const AlDesc = "Deleting a location might impact the users' configuration and leave information (e.g. the initial brought forward days)."
